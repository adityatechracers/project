.nested-row.wrapper.border-bottom.white-bg.page-heading.m-b-md
  .col-xs-12
    %h1!=t('nav.leads.sources')
    .title!= breadcrumbs({t('nav.leads.sources') => lead_sources_path}, t('shared.new'))
.row-fluid
  = render "form"