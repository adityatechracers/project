.nested-row.wrapper.border-bottom.white-bg.page-heading.m-b-md
  .col-xs-12
    %h1!= t('nav.jobs.expenses')
    .title
      != breadcrumbs({t('nav.jobs.expenses') => expenses_path}, {t('expense_categories.categories') => expense_categories_path}, t('shared.new'))

.ibox
  .ibox-title
    %h3 New expense category
  .ibox-content.p-md
    = render "expense_categories/form"
